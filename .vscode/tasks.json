// A task runner that calls the Typescript compiler (tsc) and
// Compiles a HelloWorld.ts program
{
	"version": "0.1.0",
	"command": "tsc",
	"isShellCommand": true,
	"showOutput": "always",
	"args": ["--watch","--sourcemap","--module","commonjs"],
	"problemMatcher": "$tsc"
}
